name: Translate CONTRIBUTING

on:
  push:
    branches:
      - "main"
    paths:
      - "docs/CONTRIBUTING.zh.md"
      - ".github/workflows/contributing.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 18.x

      - name: Adding CONTRIBUTING - Chinese Traditional
        uses: noHairMan/translate-readme@main
        with:
          LANG: zh-TW
          README_PATH: "docs/CONTRIBUTING.zh.md"
          OUTPUT_DIR: "docs"
          OUTPUT_FILE: "CONTRIBUTING.${lang}.md"

      - name: Adding CONTRIBUTING - English
        uses: noHairMan/translate-readme@main
        with:
          LANG: en
          README_PATH: "docs/CONTRIBUTING.zh.md"
          OUTPUT_DIR: "docs"
          OUTPUT_FILE: "CONTRIBUTING.${lang}.md"

      - name: Adding CONTRIBUTING - Japanese
        uses: noHairMan/translate-readme@main
        with:
          LANG: ja
          README_PATH: "docs/CONTRIBUTING.zh.md"
          OUTPUT_DIR: "docs"
          OUTPUT_FILE: "CONTRIBUTING.${lang}.md"
